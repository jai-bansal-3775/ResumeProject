Open Terminal:

Run command : 
1. npm init -y 

A package.json file is now created. Set the type as module :
"type" : "module"

(Run)
2. npm install express
3. npm install -g nodemon
4. npm install ejs

At last open terminal and Run
node index.js 

Server listening at http://localhost:3000 ---You will get this statement in terminal

Open chrome and run the server http://localhost:3000

